{
  "name": "demo-big-changes-code",
  "version": "1.0.0",
  "scripts": {
    "git-diff": "node ./gitDiff.js"
  },
  "git": {
    "scripts": {
      "pre-commit": "git reset && npm run git-diff && git add ."
    }
  },
  "dependencies": {
    "git-scripts": "^0.6.4"
  }
}
